const config = {
  api: {
    host: "http://localhost:",
    port: 5002,
  },
  webserver: {
    host: "http://localhost:",
    port: 3001,
  },
  db: {
    host: "http://localhost:",
    port: 3306,
  },
}

module.exports = config
